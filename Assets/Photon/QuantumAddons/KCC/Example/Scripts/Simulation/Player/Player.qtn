component Player
{
	FP JumpForce;
    FP DashForce;
    FP LastDashTime;
    int tapWindow;           
    int wTapCounter;           
    int dTapCounter;          

    bool isDashing;
    int dashFrameDuration;    
    int dashFrameTimer;

     bool lastWPressed ;     
     bool lastDPressed ;
    [HideInInspector]
    PlayerRef PlayerRef;
}
