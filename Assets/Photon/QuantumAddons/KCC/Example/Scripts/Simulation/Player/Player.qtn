component Player
{
	FP JumpForce;
    FP DashForce;
    FP LastDashTime;
    int tapWindow;           
    int wTapCounter;           
    int sTapCounter;   
    int dTapCounter;   
    int aTapCounter;   

    bool isDashing;
    int dashFrameDuration;    
    int dashFrameTimer;

    bool lastWPressed ;     
    bool lastDPressed ;
    bool lastAPressed ;
    bool lastSPressed ;

    bool isClimbing;
    FP ClimbSpeed;
    [HideInInspector]
    PlayerRef PlayerRef;
}
